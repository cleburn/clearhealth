{
  "$schema": "https://aegis.dev/schema/role.v0.1.0.json",
  "version": "0.1.0",
  "role": {
    "name": "default",
    "purpose": "Catch-all role for tasks that don't map to a specialist agent. Operates with advisory autonomy and defers to specialist agents for domain-specific work."
  },
  "scope": {
    "primary_paths": [],
    "readable_paths": [
      "**/*"
    ],
    "excluded_paths": [
      "packages/api/src/middleware/auth.ts",
      "packages/api/src/services/encryption.ts",
      "packages/api/src/middleware/pii-guard.ts"
    ]
  },
  "autonomy_overrides": {},
  "conventions": [
    "Follow all project-wide conventions: strict TypeScript, no 'any' types, conventional commits",
    "Defer to specialist agents for any work within their owned paths",
    "Log all actions to the ledger",
    "Never touch security-sensitive files (auth.ts, encryption.ts, pii-guard.ts)"
  ],
  "forbidden_actions": [
    "Modifying security-sensitive files (auth.ts, encryption.ts, pii-guard.ts)",
    "Writing to any agent's owned paths without coordination"
  ]
}
